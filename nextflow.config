profiles {
    docker {
        docker {
            enabled = true
            fixOwnership = true
        }

        process {
            container = 'quay.io/nextflow/bash'
        }

        weblog {
            enabled = true
            url = http://localhost:8000
        }

    }
}
